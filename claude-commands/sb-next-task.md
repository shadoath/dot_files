---
description: Pick up the next task from where we left off
---

Pick up where we left off. Re-read relevant files if you've lost context, check `git status` / `git diff` for the current state, and continue with the next concrete step — don't restart from scratch or re-plan unless something has clearly changed.

If you genuinely cannot tell what to resume (fresh session, no obvious in-progress task), say so briefly and ask me what to work on.
